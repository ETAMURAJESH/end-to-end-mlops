# API — required at runtime (Render installs this)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# ML — pinned to exact training environment versions
scikit-learn==1.8.0
pandas==2.3.3
numpy==2.4.6
joblib>=1.3.0

# Config
pyyaml>=6.0

# Experiment tracking
mlflow>=2.5.0
prometheus-client>=0.20.0
